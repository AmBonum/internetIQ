# AH-11.8 — Final CHANGELOG entry + admin bootstrap doc in DEPLOY_SETUP.sql

**Epic:** [AH-11 — Mock → Supabase wiring + production-ready verification](../PLAN-2026-05-17-admin-hub-integration.md)
**Effort:** `S` (≤ ½ day)
**Priority:** `P0` (production-ready gate)
**Status:** Backlog

## Goal

Last story of the integration. Land the user-visible CHANGELOG entry
announcing the `/app` + `/admin` platform, verify privacy/cookies copy
from AH-7.1 didn't drift during AH-11 wiring, and document the manual
admin-user bootstrap step at the top of `DEPLOY_SETUP.sql` so the user
can promote their first admin after running the migration in production.

## Acceptance criteria

- `CHANGELOG.md` has a new entry under the upcoming version block with:
  - Headline mentioning the new platform (`/app` test authoring, `/admin`
    panel, `/t/$shareId` respondent flow, `/s/$slug` CMS).
  - Bullet list of major user-visible additions; references the CONSENT
    `1.4.0` bump from AH-7.1.
  - Migration note pointing to `supabase/migrations/20260517000000_admin_hub_schema.sql`
    and `DEPLOY_SETUP.sql`.
- `DEPLOY_SETUP.sql` has a comment block at the top, before any SQL,
  documenting the admin bootstrap step:
  ```sql
  -- After running this file in production, promote your first admin user:
  --   INSERT INTO public.user_roles (user_id, role)
  --   VALUES ('<uuid-from-supabase-auth-users>', 'admin');
  -- Look up the uuid in the Supabase dashboard → Authentication → Users.
  -- Subsequent admins can be promoted via /admin/users once the first admin is in place.
  ```
- `src/content/legal/privacy.md` and `src/content/legal/cookies.md` verified
  unchanged since AH-7.1 (`git log` shows AH-7.1 as the last touch); if drift
  is detected, fix in this story and re-verify.
- `tasks/FEATURE_MAP-admin-hub.md` — every row Status column is Done.
- `tasks/PLAN-2026-05-17-admin-hub-integration.md` epic-map Status column
  for all 11 epics is Done.
- `npm run lint` 0/0; `npm test` green; `npm run e2e:browser` green;
  `npm run e2e:integration` green; `npm run audit:bundle` PASS.
- `tasks/AH-11-verification-report.md` (from AH-11.7) is checked in and
  shows all 15 items PASS.

## Implementation

- Edit `CHANGELOG.md`: prepend the new entry (match the existing
  `## [x.y.z] — YYYY-MM-DD` shape used by prior releases; the user-facing
  `/zmeny` page renders this file so the entry must be Slovak per CLAUDE.md
  legacy carve-out for `CHANGELOG.md`).
- Edit `DEPLOY_SETUP.sql`: insert the comment block above any existing
  content; preserve the rest verbatim.
- Run `git log --since="<AH-7.1 commit date>" -- src/content/legal/` to
  verify no drift; if drift exists, restore intended copy.
- Update `tasks/FEATURE_MAP-admin-hub.md` and the Epic Map Status column
  in `tasks/PLAN-2026-05-17-admin-hub-integration.md` for the last
  remaining rows.

## Tests

- Vitest snapshot at `tests/changelog.test.ts` (if pattern exists) confirms
  the latest entry parses; otherwise rely on the `/zmeny` route rendering
  the new entry — assert via the existing Playwright spec for that route
  (extend if needed).
- No new e2e specs required; the integration's full coverage is owned by
  the per-feature epics.

## Documentation

- `CHANGELOG.md` — the entry itself.
- `DEPLOY_SETUP.sql` — the bootstrap comment block.
- `tasks/FEATURE_MAP-admin-hub.md` and `tasks/PLAN-...md` — Status columns
  all Done.

## Code review

Fresh-context prompt: reviewer opens `CHANGELOG.md` and reads the entry —
clear and accurate? Opens `DEPLOY_SETUP.sql` and confirms the bootstrap
comment is at the top, not buried. Confirms no drift in legal pages.
Confirms FEATURE_MAP + PLAN epic map are fully Done. Final smoke:
`npm run lint && npm test && npm run build && npm run audit:bundle` all
green on the reviewer's machine.

## Dependencies

- **Depends on:** AH-11.7 (the report must exist and pass)
- **Source in admin-hub:** N/A
