# AH-10.2 — Admin dashboard (`/admin`) with PageHeader + StatCard summary

**Epic:** [AH-10 — Admin panel role-gating + shell](../PLAN-2026-05-17-admin-hub-integration.md#epic-map)
**Effort:** `S`
**Priority:** `P1`
**Status:** 🟡 Ready
**Depends on:** AH-10.1, AH-3.2 (StatCard ported)
**Source in admin-hub:** `routes/admin/index.tsx`, `components/admin/PageHeader.tsx`

## Goal
Port the platform admin landing page at `/admin` with a PageHeader and a row of StatCard tiles (total users, total tests, total sessions, pending DSRs).

## Acceptance criteria
- `src/routes/admin/index.tsx` renders under the `admin` layout (inherits `requireSupabaseAuth` + `requireRole('admin')`).
- `src/components/admin/PageHeader.tsx` ported (distinct from `components/app/page-header.tsx` ported in AH-3.1).
- All ported files run through `npx eslint --fix`.
- Data-testids: `admin-dashboard-page-header`, `admin-stat-card-users`, `admin-stat-card-tests`, `admin-stat-card-sessions`, `admin-stat-card-dsr-pending`, `admin-dashboard-recent-activity`.
- Slovak strings extracted to `src/i18n/locales/sk/admin.json` under `dashboard.*`.
- Reads from `src/lib/admin/mock-store.ts` only; zero Supabase calls.
- FEATURE_MAP-admin-hub.md status for affected rows marked Done with `see git log`.
- CHANGELOG entry under upcoming version (user-visible: yes).

## Implementation
- `cp admin-hub/src/routes/admin/index.tsx src/routes/admin/index.tsx`
- `cp admin-hub/src/components/admin/PageHeader.tsx src/components/admin/PageHeader.tsx`
- Path rewrites: `@/lib/admin/store` → `@/lib/admin/mock-store`; `@/lib/admin-mock-data` → `@/lib/admin/mock-data`.
- `npx eslint --fix` per copied file.
- Append `dashboard.*` keys to `src/i18n/locales/sk/admin.json`.

## Tests
- Vitest at `tests/routes/admin/index.test.tsx` — happy render with seeded mock store; empty-state edge when mock store returns no records.
- Vitest at `tests/components/admin/PageHeader.test.tsx` — renders title + actions slot.
- Playwright spec `e2e/specs/admin/dashboard.spec.ts` with POM `e2e/poms/admin/AdminDashboardPage.ts`. POM-only locators; assert all four StatCards visible.

## Documentation
- `tasks/FEATURE_MAP-admin-hub.md` — mark relevant rows Done with `see git log`.
- `CHANGELOG.md` — entry under upcoming version (user-visible: yes).
- `privacy/cookies` — N/A.

## Code review (fresh context)
Reviewer verifies: no Supabase calls; data-testids present; i18n keys present; PageHeader has no admin-only copy baked in (translatable). Files: `src/routes/admin/index.tsx`, `src/components/admin/PageHeader.tsx`, `src/i18n/locales/sk/admin.json`.
