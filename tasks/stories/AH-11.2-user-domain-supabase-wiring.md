# AH-11.2 — Replace mock-store with React Query hooks for USER (/app/*) domain

**Epic:** [AH-11 — Mock → Supabase wiring + production-ready verification](../PLAN-2026-05-17-admin-hub-integration.md)
**Effort:** `L` (≤ 1 week)
**Priority:** `P1` (must)
**Status:** Backlog

## Goal

Swap every `from "@/lib/platform/mock-store"` and
`from "@/lib/platform/mock-user-data"` import across the `/app/*` creator
domain (tests, sets, library, audiences, teams, templates, history,
notifications, account) for TanStack Query hooks against Supabase.

## Acceptance criteria

- All ported files have `npx eslint --fix` applied; `npm run lint` is 0/0.
- `grep -r "from \"@/lib/platform/mock-store\"" src/` returns zero matches.
- `grep -r "from \"@/lib/platform/mock-user-data\"" src/` returns zero matches.
- No new mock files created; mock files are DELETED in AH-11.6.
- `data-testid` values preserved from AH-3..AH-7; no new test-ids added.
- i18n keys unchanged; no new namespaces.
- Every query keyed on `["app", <domain>, ...args]`; mutations invalidate
  the matching prefix.
- Account profile / DSR mutations call the AH-11.3 privileged server fns
  rather than the anon client.
- `FEATURE_MAP-admin-hub.md` rows for each swapped `/app/*` component
  marked Done in the same commit.
- CONSENT_VERSION unchanged (1.4.0 from AH-7.1).

## Implementation

- Hooks live at `src/lib/platform/queries.ts` grouped by domain
  (`useAppTests`, `useAppLibrary`, `useAppAudiences`, ...).
- Edit every `src/routes/app/**/*.tsx` and `src/components/app/**/*.tsx`
  that imports `mock-store` or `mock-user-data`.
- Reads use the anon Supabase client; writes that touch
  governance-sensitive surface (DSR submit, profile, role-bearing tables)
  call the AH-11.3 server fns.

## Tests

- Existing Vitest + Playwright suites from AH-3..AH-7 pass unchanged.
- Add `e2e/integration/admin-hub/app-<domain>-supabase.spec.ts` per
  domain, gated on `SUPABASE_INTEGRATION=1`, against the preview DB.

## Documentation

- `tasks/FEATURE_MAP-admin-hub.md` — user-domain rows marked Done.

## Code review

Fresh-context prompt: confirm zero `mock-store` / `mock-user-data`
imports remain under `src/routes/app/` or `src/components/app/`; DSR
submit and profile update routes call AH-11.3 server fns, not the anon
client directly.

## Dependencies

- **Depends on:** AH-3, AH-4, AH-5, AH-7, AH-11.3
- **Source in admin-hub:** `admin-hub/docs/INTEGRATION.md` steps 5–7;
  `admin-hub/src/lib/platform/store.ts` (replaced).
