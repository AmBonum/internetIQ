# AH-10.4 — Admin support (`/admin/support`) + support-config lib

**Epic:** [AH-10 — Admin panel role-gating + shell](../PLAN-2026-05-17-admin-hub-integration.md#epic-map)
**Effort:** `S`
**Priority:** `P2`
**Status:** 🟡 Ready
**Depends on:** AH-10.1
**Source in admin-hub:** `routes/admin/support.tsx`, `lib/admin/support-config.ts`

## Goal
Port the admin support page that edits contact channels (email, phone, hours) plus the `support-config` lib that holds the channel data shape and validation helpers.

## Acceptance criteria
- `src/routes/admin/support.tsx` renders under the `admin` layout (inherits role gate).
- `src/lib/admin/support-config.ts` ported with its existing exports (types + helpers).
- All ported files run through `npx eslint --fix`.
- Data-testids: `admin-support-page-header`, `admin-support-form`, `admin-support-email-input`, `admin-support-phone-input`, `admin-support-hours-input`, `admin-support-submit`, `admin-support-toast-success`, `admin-support-channel-list`.
- Slovak strings extracted to `src/i18n/locales/sk/admin.json` under `support.*`.
- Reads/writes via `src/lib/admin/mock-store.ts` only; zero Supabase calls.
- FEATURE_MAP-admin-hub.md status for `routes/admin/support.tsx` and `lib/admin/support-config.ts` marked Done with `see git log`.
- CHANGELOG entry under upcoming version (user-visible: yes).

## Implementation
- `cp admin-hub/src/routes/admin/support.tsx src/routes/admin/support.tsx`
- `cp admin-hub/src/lib/admin/support-config.ts src/lib/admin/support-config.ts`
- Path rewrites: `@/lib/admin/store` → `@/lib/admin/mock-store`; `@/lib/admin-mock-data` → `@/lib/admin/mock-data`.
- `npx eslint --fix` per copied file.
- Append `support.*` keys to `src/i18n/locales/sk/admin.json`.

## Tests
- Vitest at `tests/routes/admin/support.test.tsx` — happy render with seeded config; edit + submit calls mock store updater; validation edge (invalid email → error visible).
- Vitest at `tests/lib/admin/support-config.test.ts` — exported helpers (email/phone validation) behave as documented.
- Playwright spec `e2e/specs/admin/support.spec.ts` with POM `e2e/poms/admin/AdminSupportPage.ts`. POM-only locators; verify submit shows success toast.

## Documentation
- `tasks/FEATURE_MAP-admin-hub.md` — mark relevant rows Done with `see git log`.
- `CHANGELOG.md` — entry under upcoming version (user-visible: yes).
- `privacy/cookies` — N/A.

## Code review (fresh context)
Reviewer verifies: no Supabase calls; data-testids present; i18n keys present; `support-config.ts` is a pure lib (no React imports). Files: `src/routes/admin/support.tsx`, `src/lib/admin/support-config.ts`.
