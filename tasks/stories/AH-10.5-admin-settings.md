# AH-10.5 — Admin settings (`/admin/settings`) — UI only

**Epic:** [AH-10 — Admin panel role-gating + shell](../PLAN-2026-05-17-admin-hub-integration.md#epic-map)
**Effort:** `S`
**Priority:** `P2`
**Status:** 🟡 Ready
**Depends on:** AH-10.1
**Source in admin-hub:** `routes/admin/settings.tsx`

## Goal
Port the admin settings page (feature flags, retention defaults, branding tokens) — UI only. **`app_settings` CRUD backend is deferred** per PLAN-2026-05-17 Out-of-Scope §5. Form submit shows a "not yet persisted" toast; values reset on refresh.

## Acceptance criteria
- `src/routes/admin/settings.tsx` renders under the `admin` layout (inherits role gate).
- All ported files run through `npx eslint --fix`.
- Submit button is visible but persists nothing beyond an in-memory `useState`; tooltip / inline copy says backend deferred. **Call out in PR description.**
- Data-testids: `admin-settings-page-header`, `admin-settings-form`, `admin-settings-feature-flag-${flag}`, `admin-settings-retention-default-input`, `admin-settings-branding-primary-color`, `admin-settings-submit`, `admin-settings-deferred-notice`, `admin-settings-toast-info`.
- Slovak strings extracted to `src/i18n/locales/sk/admin.json` under `settings.*` (including the deferred-backend notice).
- Reads from `src/lib/admin/mock-store.ts` only; zero Supabase calls.
- FEATURE_MAP-admin-hub.md status for `routes/admin/settings.tsx` marked Done with `see git log`.
- CHANGELOG entry under upcoming version (user-visible: yes; note backend deferred).

## Implementation
- `cp admin-hub/src/routes/admin/settings.tsx src/routes/admin/settings.tsx`
- Path rewrites: `@/lib/admin/store` → `@/lib/admin/mock-store`; `@/lib/admin-mock-data` → `@/lib/admin/mock-data`.
- `npx eslint --fix src/routes/admin/settings.tsx`.
- Append `settings.*` keys to `src/i18n/locales/sk/admin.json`.

## Tests
- Vitest at `tests/routes/admin/settings.test.tsx` — renders form; submit shows "not yet persisted" toast; deferred notice visible.
- Playwright spec `e2e/specs/admin/settings.spec.ts` with POM `e2e/poms/admin/AdminSettingsPage.ts`. POM-only locators; verify deferred notice visible and submit shows info toast.

## Documentation
- `tasks/FEATURE_MAP-admin-hub.md` — mark relevant row Done with `see git log`.
- `CHANGELOG.md` — entry under upcoming version (user-visible: yes); explicit "Settings UI present, `app_settings` CRUD deferred" note.
- `privacy/cookies` — N/A.

## Code review (fresh context)
Reviewer verifies: no Supabase calls; data-testids present; i18n keys present; submit is a no-op toast, not a real persistence call; deferred-notice visible. File: `src/routes/admin/settings.tsx`.
