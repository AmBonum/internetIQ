# AH-10.3 — Admin users list (`/admin/users`)

**Epic:** [AH-10 — Admin panel role-gating + shell](../PLAN-2026-05-17-admin-hub-integration.md#epic-map)
**Effort:** `S`
**Priority:** `P1`
**Status:** 🟡 Ready
**Depends on:** AH-10.1
**Source in admin-hub:** `routes/admin/users.tsx`

## Goal
Port the platform admin users table: list profiles with role badges, search/filter, and role-edit action stub.

## Acceptance criteria
- `src/routes/admin/users.tsx` renders under the `admin` layout (inherits role gate).
- All ported files run through `npx eslint --fix`.
- Data-testids: `admin-users-page-header`, `admin-users-search-input`, `admin-users-role-filter`, `admin-users-table`, `admin-users-row-${userId}`, `admin-users-role-badge-${userId}`, `admin-users-edit-role-${userId}`, `admin-users-empty-state`.
- Slovak strings extracted to `src/i18n/locales/sk/admin.json` under `users.*`.
- Reads/writes via `src/lib/admin/mock-store.ts` only; zero Supabase calls.
- FEATURE_MAP-admin-hub.md status for `routes/admin/users.tsx` marked Done with `see git log`.
- CHANGELOG entry under upcoming version (user-visible: yes).

## Implementation
- `cp admin-hub/src/routes/admin/users.tsx src/routes/admin/users.tsx`
- Path rewrites: `@/lib/admin/store` → `@/lib/admin/mock-store`; `@/lib/admin-mock-data` → `@/lib/admin/mock-data`.
- `npx eslint --fix src/routes/admin/users.tsx`.
- Append `users.*` keys to `src/i18n/locales/sk/admin.json`.

## Tests
- Vitest at `tests/routes/admin/users.test.tsx` — happy render with seeded users; search filter narrows rows; role filter narrows rows; empty-state edge.
- Playwright spec `e2e/specs/admin/users.spec.ts` with POM `e2e/poms/admin/AdminUsersPage.ts`. POM-only locators; verify role badge visible per row.

## Documentation
- `tasks/FEATURE_MAP-admin-hub.md` — mark relevant row Done with `see git log`.
- `CHANGELOG.md` — entry under upcoming version (user-visible: yes).
- `privacy/cookies` — N/A (mock-only; AH-11 wires real user reads with audit log).

## Code review (fresh context)
Reviewer verifies: no Supabase calls; data-testids present; i18n keys present; role-edit action calls only mock store updater. File: `src/routes/admin/users.tsx`.
