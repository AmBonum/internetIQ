# E12.3 — Respondent intake form (name + email + GDPR consent)

**Epic:** [E12 — Education mode](../PLAN-2026-04-26-custom-tests-sponsorship.md#epic-12--education-mode-autori-zbierajú-výsledky)
**Effort:** `S` (≤ ½ dňa)
**Priority:** `P2` (should)
**Status:** ✅ Done (2026-05-01)

## Kontext

Keď respondent (študent / zamestnanec) otvorí edu test, pred štartom
musí poskytnúť meno, email a explicitne súhlasiť so spracovaním PII.
Bez splnenia oboch nemôže spustiť test.

## User story

> Ako študent absolvujúci test, ktorý mi poslal učiteľ, **chcem** rozumieť
> aké údaje o mne ukladá a komu, **aby** som mal informovaný súhlas pred
> tým než začnem.

## Acceptance criteria

- **AC-1**: Route `/test/zostava/$id` po načítaní pacaku zistí
  `test_set.collects_responses`. Ak true → zobrazí intake form pred
  test-flow start. Ak false → správanie ako E8.3 (anon).
- **AC-2**: Intake form polia:
  - „Meno a priezvisko" (povinné, 2-80 znakov)
  - „Email" (povinné, server-side regex validácia)
  - **GDPR consent checkbox**: „Súhlasím so spracovaním môjho mena a
    emailu pre účely vyhodnotenia testu autorom. Údaje sa uchovávajú
    najviac 12 mesiacov. Viac v [zásadách spracovania](privacy)."
  - **Honeypot field** (invisible, anti-bot): `<input name="hp_url"
    aria-hidden="true" tabindex="-1" style="position:absolute;left:-9999px">`
    — server reject ak nie prázdne (E12.7)
- **AC-3**: Submit:
  - Klient validuje email regex + meno length
  - POST do `/api/begin-edu-attempt` s `set_id, name, email, consent_at`
  - Server overí honeypot, rate limit (E12.7), existenciu set_id
  - Vráti `attempt_token` (signed JWT, 60 min TTL) — token sa použije
    pri finále testu pri INSERT attempts row
- **AC-4**: Po úspechu → mountuje `<TestFlow config={ kind: "composer", ... }>`
  s `attempt_token` do scope. Po dokončení testu sa token + meno + email
  pošlú do INSERT path.
- **AC-5**: Disclosure pred formom (vidteľné prevádzkovateľa):
  - „Test pripravil: {test_set.creator_label || 'autor'}"
  - „Tvoj autor uvidí tvoje meno, email, skóre a odpovede."
  - „Údaje sa uchovávajú 12 mesiacov, potom sa anonymizujú."
- **AC-6**: A11y — `<form aria-labelledby>`, error messages v
  `aria-live="polite"`, povinné polia označené `aria-required`.
- **AC-7**: Mobile-first.

## Subtasks

### Implementation
- [x] [`src/components/composer/RespondentIntakeForm.tsx`](../../src/components/composer/RespondentIntakeForm.tsx) — name + email + GDPR consent checkbox + visually-hidden honeypot, plus disclosure paragraph (autor, retention, privacy link).
- [x] [`functions/api/begin-edu-attempt.ts`](../../functions/api/begin-edu-attempt.ts) — honeypot, rate-limit (3/5min/IP + 50/h/set), email regex, name length, set lookup, dupe detection, signed JWT issuance.
- [x] [`functions/_lib/jwt.ts`](../../functions/_lib/jwt.ts) — Web Crypto HMAC-SHA-256, env `EDU_JWT_SECRET`, 60-min TTL.
- [x] Conditional intake gating v [`src/routes/test.zostava.$id.tsx`](../../src/routes/test.zostava.$id.tsx) — pri `collects_responses=true` sa `<RespondentIntakeForm>` renderuje pred `setStarted(true)`.
- [x] Server-mediated INSERT cez [`functions/api/finish-edu-attempt.ts`](../../functions/api/finish-edu-attempt.ts) — JWT verify + service-role INSERT; respondent fields zo claims, nie z body (test asserne, že spoofnuté `respondent_*` v body sa nepouzili).
- [x] RLS lockdown migrácia [`20260501010000_edu_anon_insert_lockdown.sql`](../../supabase/migrations/20260501010000_edu_anon_insert_lockdown.sql) — anon nemôže INSERT row s `respondent_*`.
- [x] [`src/components/quiz/TestFlow.tsx`](../../src/components/quiz/TestFlow.tsx) + [`src/components/quiz/ResultsView.tsx`](../../src/components/quiz/ResultsView.tsx) — `edu` prop preneseny cez chain; `persistResult` route cez `/api/finish-edu-attempt` keď `edu` set.

### Tests
- [x] [`tests/components/composer/RespondentIntakeForm.test.tsx`](../../tests/components/composer/RespondentIntakeForm.test.tsx) (5) — disclosure render, validation gate (disabled until name+email+consent), happy POST → onReady, aria-live error, honeypot off-screen + tabindex=-1.
- [x] [`tests/functions/begin-edu-attempt.test.ts`](../../tests/functions/begin-edu-attempt.test.ts) (10) — happy path, honeypot, invalid_email, name_length, missing consent, set_not_found, not_edu_set, already_attempted, rate_limited, jwt_not_configured.
- [x] [`tests/functions/finish-edu-attempt.test.ts`](../../tests/functions/finish-edu-attempt.test.ts) (6) — JWT-from-claims wins over spoofed body, bad_signature, expired, missing_token, invalid share_id, rate_limited.
- [x] [`tests/functions/jwt.test.ts`](../../tests/functions/jwt.test.ts) (5) — round-trip, bad signature, malformed, expired, tampered payload.
- [x] [`tests/db/edu-rls-lockdown.test.ts`](../../tests/db/edu-rls-lockdown.test.ts) (3) — DROP staré, CREATE nové s `respondent_* IS NULL` CHECK, žiadny anon UPDATE/DELETE policy.

### Documentation
- [x] Privacy z E12.6 už referuje tento flow (sekcia 8 „Education mode").

### Code review (fresh context)
- [x] Consent checkbox is `aria-required` and gates submit button — pokrýva component test.
- [x] Disclosure is rendered above form fields (poradie v JSX zachytené aj v teste *„renders disclosure mentioning author + 12-month retention"*).

## Závislosti

- **Blokuje:** E12.1 (schéma), E12.7 (anti-spam)

## Riziká

| Riziko | Mitigácia |
|---|---|
| Bot filluje real email niekoho iného | E12.7 rate limit + honeypot; pri repeated abuse autor môže manuálne zmazať respondent z dashboardu |
| JWT secret leak | `JWT_SECRET` env var na CF Pages, rotated quarterly |

## Definition of Done

Pozri [`tasks/README.md`](../README.md#definition-of-done-každá-story).
