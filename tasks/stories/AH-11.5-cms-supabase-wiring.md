# AH-11.5 — CMS Supabase wiring (admin/pages, header, footer, navigation, /s/$slug)

**Epic:** [AH-11 — Mock → Supabase wiring + production-ready verification](../PLAN-2026-05-17-admin-hub-integration.md)
**Effort:** `M` (≤ 2 days)
**Priority:** `P1` (must)
**Status:** Backlog

## Goal

Wire the CMS surface — admin pages list/editor, header/footer/navigation
config, and the public `/s/$slug` renderer — to Supabase, replacing the
mock TODOs from AH-9.

## Acceptance criteria

- All ported files have `npx eslint --fix` applied; `npm run lint` is 0/0.
- `grep -r "from \"@/lib/admin/cms-mock-store\"" src/` returns zero matches.
- `data-testid` values preserved from AH-9; no new test-ids added.
- i18n keys unchanged; the AH-9 `cms` namespace is unmodified.
- `/s/$slug` server fn at `src/lib/cms/get-page.functions.ts` uses
  `supabaseAdmin` with explicit safe-column projection; published pages
  (`published_at IS NOT NULL`) render normally; unpublished returns 404.
- Admin CMS list / editor / header / footer / navigation routes use the
  `src/lib/cms/queries.ts` hook module; mutations call privileged server
  fns (AH-11.3) where an audit-log entry is required.
- `FEATURE_MAP-admin-hub.md` CMS rows marked Done.
- CONSENT_VERSION unchanged (1.4.0 from AH-7.1).

## Implementation

- Fill in the skeleton at `src/lib/cms/get-page.functions.ts` (AH-9.7).
- New hook module `src/lib/cms/queries.ts` for admin CMS list/editor;
  pattern matches `src/lib/admin/queries.ts`.
- Public `/s/$slug` route loader calls the server fn; safe-column
  projection excludes draft-only fields.
- Header / footer / navigation config write paths invoke an AH-11.3
  server fn so the change is audit-logged.

## Tests

- Existing Vitest + Playwright suites from AH-9 pass unchanged.
- Add `e2e/integration/admin-hub/cms-supabase.spec.ts`, gated on
  `SUPABASE_INTEGRATION=1`, exercising publish → render → unpublish
  → 404 against the preview DB.

## Documentation

- `tasks/FEATURE_MAP-admin-hub.md` — CMS rows Done.

## Code review

Fresh-context prompt: published-only filter enforced server-side;
header/footer/navigation writes are audit-logged; no `cms-mock-store`
imports remain.

## Dependencies

- **Depends on:** AH-9, AH-11.3
- **Source in admin-hub:** `admin-hub/docs/INTEGRATION.md` step 10.
